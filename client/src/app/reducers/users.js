const users = (state = {}, action) => {
  switch (action.type) {
    case 'ADD_USER':
      // NEED TO UPDATE BASED ON USER SCHEMA
      return state

    default:
      return state
  }
}

export default users